{
  "name": "ferg-layout-challenge",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "parcel": "^1.12.4",
    "sass": "^1.30.0"
  },
  "scripts": {
    "dev": "parcel serve src/index.html --port 8080",
    "watch": "parcel serve watch src/index.html --port 8080 --open",
    "build": "parcel build src/index.html"
  },
  "dependencies": {
    "bulma": "^0.9.1",
    "normalize.css": "^8.0.1"
  }
}
